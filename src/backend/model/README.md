## Hướng dẫn chạy

Trong thư mục model, chạy câu lệnh sau:

```
node trainer.js
```

Module sẽ tiến hành huấn luyện mô hình dựa trên bộ dataset `water_potability.csv`. Tập tin weights sẽ được lưu vào thư mục `data`.
