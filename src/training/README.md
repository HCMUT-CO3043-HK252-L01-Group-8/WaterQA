## Hướng dẫn chạy

Trong thư mục training, chạy câu lệnh sau:

```
npx http-server .
```

Mở `http://127.0.0.1:8080/` trong trình duyệt. Trang sẽ bắt đầu huấn luyện từ bộ dữ liệu.

Khi hoàn tất, trang chủ sẽ hiện nút để tải model về.
